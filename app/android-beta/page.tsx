import { AndroidBeta } from "@/components/AndroidBeta";

export const metadata = {
  title: "Threadbase — Android beta",
  description:
    "Join the Threadbase Android closed-alpha beta on Google Play. Two clicks, a short wait, and you're testing the mobile companion to Claude Code.",
};

export default function AndroidBetaPage(): React.JSX.Element {
  return <AndroidBeta />;
}
